# Monitoring-Detecting-security-issues-using-AWS-config
This lab focused on using AWS Config to continuously monitor the configuration of my AWS resources. While CloudTrail tells you who changed something, AWS Config tells you what the resource looks like now and if it follows security best practices. It acts as an automated security auditor that flags resources the moment they become insecure.
